# main.py 仕様

## 目的
Pygame 画像ビューアのエントリーポイント。

## 役割
- `src.game` から `Game` を import し、アプリを開始する。
- `main()` 関数と `__main__` ガードを提供する。

## 関数
- `main() -> None`: `Game` を生成し `run()` を呼ぶ。

## 入出力
- 入力: なし（CLI 実行のみ）。
- 出力: なし（実処理は `Game` に委譲）。

## 依存
- `src.game.Game`
- Python 標準ランタイム

## エラーハンドリング
- 本ファイル内では特に行わない（挙動は `Game` に委譲）。
